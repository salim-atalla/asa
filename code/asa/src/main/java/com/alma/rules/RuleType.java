package com.alma.rules;

/**
 * Enum representing different types of rules that can be applied to services.
 */
public enum RuleType {
    NAME_MATCH, // Rule for matching names of services
    COMPATIBILITY_CHECK // Rule for checking compatibility of services
}
